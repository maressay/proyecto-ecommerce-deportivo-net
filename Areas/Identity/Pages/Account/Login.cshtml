@page
@model LoginModel

@{
    ViewData["Title"] = "Iniciar Sesión";
}

<<<<<<< HEAD
<head>
    <link rel="stylesheet" href="~/css/formularios.css" />
    <link rel="stylesheet" href="~/css/login.css" />
</head>

<div class="formulario">
    <form method="post">
        <div id="title">
            <h1>@ViewData["Title"]</h1>
        </div>
        <hr/ id="line">
        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
        <div class="inputBox">
            <div class="input" id="input-email">
                <span>Email</span>
                <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true"
                    placeholder="name@example.com" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
                <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            </div>
        </div>

        <div class="inputBox">
            <div class="input">
                <span>Contraseña</span>
                <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true"
                    placeholder="password" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
        </div>
        
        <div class="inputBox">
            <div class="checkbox">
                <label asp-for="Input.RememberMe" class="form-label">
                    <input class="form-check-input" asp-for="Input.RememberMe" />
                        @Html.DisplayNameFor(m => m.Input.RememberMe)
                </label>
            </div>
        </div>

        <input type="submit" value="Ingresar" class="btn"></input>

        <div class="login-options">
            <div class="input">
                <a id="forgot-password" asp-page="./ForgotPassword">¿Olvidaste tu contraseña?</a>
            </div>
            <div class="input">
                <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Registrarse como nuevo usuario</a>
            </div>
            <div class="input">
                <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">Reenviar confirmación por correo
                    electrónico</a>
            </div>
        </div>
    </form>
=======
<style>
    .contenedor-login {
        width: 50%;
        margin: 0 auto;
    }

    @@media (width <=800px) {
        .contenedor-login {
            width: 100%;
            margin: 0 auto;
        }
    }
</style>

<div class="row contenedor-login">
    <div class="col-md-7">
        <section>
            <form id="account" method="post">
                <h1>@ViewData["Title"]</h1>
                <h2>Utilice una cuenta local para iniciar sesión.</h2>
                <hr />
                <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                <div class="form-floating mb-3">
                    <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true"
                        placeholder="name@example.com" />
                    <label asp-for="Input.Email" class="form-label">Email</label>
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <div class="form-floating mb-3">
                    <input asp-for="Input.Password" class="form-control" autocomplete="current-password"
                        aria-required="true" placeholder="password" />
                    <label asp-for="Input.Password" class="form-label">Password</label>
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div class="checkbox mb-3">
                    <label asp-for="Input.RememberMe" class="form-label" style="margin-left: 15px;">
                        <input class="form-check-input" asp-for="Input.RememberMe" />
                        @Html.DisplayNameFor(m => m.Input.RememberMe)
                    </label>
                </div>
                <div>
                    <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary">Log in</button>
                </div>
                <br>
                <div>
                    <p>
                        <a id="forgot-password" asp-page="./ForgotPassword">¿Olvidaste tu contraseña?</a>
                    </p>
                    <p>
                        <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Registrarse como nuevo
                            usuario</a>
                    </p>
                    <p>
                        <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">Reenviar confirmación por
                            correo electrónico</a>
                    </p>
                </div>
            </form>
        </section>
    </div>
    <div class="col-md-5 col-md-offset-2">
>>>>>>> 733d700abb4384551ef342092c287b9de1064f1a
        <section>
            <h3>Utilice otro servicio para iniciar sesión.</h3>
            <hr />
            @{
                if ((Model.ExternalLogins?.Count ?? 0) == 0)
                {
                    <div>
                        <p>
                            No hay servicios de autenticación externos configurados. Mira esto <a
                                href="https://go.microsoft.com/fwlink/?LinkID=532715">artículo
                                acerca de la configuración de esta aplicación ASP.NET para admitir el inicio de sesión a través
                                de servicios externos</a>.
                        </p>
                    </div>
                }
                else
                {
                    <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post"
                        class="form-horizontal">
                        <div>
                            <p>
                                @foreach (var provider in Model.ExternalLogins!)
                                {
                                    <button type="submit" class="btn btn-primary" name="provider" value="@provider.Name"
                                        title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>
                                }
                            </p>
                        </div>
                    </form>
                }
            }
        </section>



</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
